spring:
     datasource:
       username: root
       password: aditya11
       url: jdbc:mysql://localhost:3306/hostel_management_project
       driver-class-name: com.mysql.cj.jdbc.Driver
     jpa:
         hibernate:
            ddl-auto: update
         show-sql: true
         properties:
            hibernate:
              format_sql: true
              jdbc:
                 lob:
              non_contextual_creation: true
              database: mysql
            database-platform: org.hibernate.dialect.MySQLDialect
logging:
  level:
    root: INFO
    com:
      example:
        hostel_management: INFO
